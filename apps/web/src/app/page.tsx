import { APP_NAME } from "@sparkmind/shared";

/**
 * Placeholder page — BUKAN landing page.
 *
 * Sprint 002 (Engineering Foundation) hanya menyiapkan workspace.
 * Landing page dibangun pada sprint produk berikutnya.
 */
export default function HomePage() {
  return (
    <main>
      <h1>{APP_NAME}</h1>
      <p>Workspace scaffold — landing page menyusul di sprint berikutnya.</p>
    </main>
  );
}
